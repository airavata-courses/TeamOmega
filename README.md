# TeamOmega

###Build the package
    cd regsitry-api
    mvn package

###Run the server
    mvn exec:java

### Prerequisites:
Make sure that the following are installed on the instance.
    maven
    jdk 1.8
    code deploy
