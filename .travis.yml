language: python
python:
  - "2.7"
cache: pip
before
install:
  - cd Data_Ingestor
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - pip install -r requirements.txt
script:
  - nosetests UnitTests.py
