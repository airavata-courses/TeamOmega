version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/TeamOmega
hooks:
  BeforeInstall: 
    - location: startup_scripts/before_install.sh
      runas: root
      timeout: 300
  ApplicationStart:
    - location: startup_scripts/install.sh
      runas: root
      timeout: 240
